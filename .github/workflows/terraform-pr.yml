name: 'Terraform PR Validation'

on:
  pull_request:
    branches:
      - main
      - master
    paths:
      - 'tf/**'
      - '.github/workflows/terraform-*.yml'

env:
  TF_VERSION: '1.15.3'
  AWS_REGION: 'us-east-1'
  TF_WORKING_DIR: 'tf'

permissions:
  contents: read
  pull-requests: write

jobs:
  terraform-validate:
    name: 'Terraform Validate'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout Code'
        uses: actions/checkout@v4

      - name: 'Setup Terraform'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: 'Terraform Format Check'
        id: fmt
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform fmt -check -recursive -diff
        continue-on-error: true

      - name: 'Terraform Init'
        id: init
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform init -backend=false

      - name: 'Terraform Validate'
        id: validate
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform validate -no-color

      - name: 'Security Scan with tfsec'
        id: tfsec
        uses: aquasecurity/tfsec-action@v1.0.3
        with:
          working_directory: ${{ env.TF_WORKING_DIR }}
          soft_fail: true

      - name: 'Upload Security Scan Results'
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: ${{ env.TF_WORKING_DIR }}/tfsec-results.sarif
        continue-on-error: true

      - name: 'Comment PR - Format Issues'
        if: steps.fmt.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ **Terraform Format Check Failed**\n\nPlease run `terraform fmt -recursive` to fix formatting issues.'
            })

      - name: 'Comment PR - Validation Summary'
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const outcome = '${{ steps.validate.outcome }}';
            const status = outcome === 'success' ? '✅' : '❌';
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `${status} **Terraform Validation**: ${outcome}`
            })

  terraform-plan-dev:
    name: 'Terraform Plan - Dev'
    needs: terraform-validate
    runs-on: ubuntu-latest
    # Note: Configure 'dev' environment in GitHub repo settings if environment protection is needed
    
    steps:
      - name: 'Checkout Code'
        uses: actions/checkout@v4

      - name: 'Setup Terraform'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: 'Configure AWS Credentials'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          ## TODO: Setup AWS Role OIDC
          # role-to-assume: ${{ secrets.AWS_ROLE_ARN_DEV }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 'Terraform Init'
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform init -input=false

      - name: 'Terraform Plan'
        id: plan
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform plan -input=false -var-file="envs/terraform_dev.tfvars" -out=tfplan-dev -no-color  | tee plan-output.txt
        continue-on-error: true

      - name: 'Upload Plan Artifact'
        if: steps.plan.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan-dev
          path: ${{ env.TF_WORKING_DIR }}/tfplan-dev
          retention-days: 5

      - name: 'Comment PR with Plan'
        if: steps.plan.outcome == 'success'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const planOutput = fs.readFileSync('${{ env.TF_WORKING_DIR }}/plan-output.txt', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 📋 Terraform Plan - Dev Environment\n\n\`\`\`\n${planOutput}\n\`\`\``
            })

  terraform-plan-staging:
    name: 'Terraform Plan - Staging'
    needs: terraform-validate
    runs-on: ubuntu-latest
    # Note: Configure 'staging' environment in GitHub repo settings if environment protection is needed
    
    steps:
      - name: 'Checkout Code'
        uses: actions/checkout@v4

      - name: 'Setup Terraform'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      # - name: 'Configure AWS Credentials'
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: ${{ secrets.AWS_ROLE_ARN_STAGING }}
      #     aws-region: ${{ env.AWS_REGION }}

      - name: 'Terraform Init'
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform init -input=false

      - name: 'Terraform Plan'
        id: plan
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform plan -input=false -var-file="envs/terraform_staging.tfvars" -out=tfplan-staging
        continue-on-error: true

      - name: 'Upload Plan Artifact'
        if: steps.plan.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan-staging
          path: ${{ env.TF_WORKING_DIR }}/tfplan-staging
          retention-days: 5

  terraform-plan-test:
    name: 'Terraform Plan - Test'
    needs: terraform-validate
    runs-on: ubuntu-latest
    # Note: Configure 'test' environment in GitHub repo settings if environment protection is needed
    
    steps:
      - name: 'Checkout Code'
        uses: actions/checkout@v4

      - name: 'Setup Terraform'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      # - name: 'Configure AWS Credentials'
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: ${{ secrets.AWS_ROLE_ARN_TEST }}
      #     aws-region: ${{ env.AWS_REGION }}

      - name: 'Terraform Init'
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform init -input=false

      - name: 'Terraform Plan'
        id: plan
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform plan -input=false -var-file="envs/terraform_test.tfvars" -out=tfplan-test
        continue-on-error: true

      - name: 'Upload Plan Artifact'
        if: steps.plan.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan-test
          path: ${{ env.TF_WORKING_DIR }}/tfplan-test
          retention-days: 5

  terraform-plan-prod:
    name: 'Terraform Plan - Prod'
    needs: terraform-validate
    runs-on: ubuntu-latest
    # Note: Configure 'prod' environment in GitHub repo settings if environment protection is needed
    
    steps:
      - name: 'Checkout Code'
        uses: actions/checkout@v4

      - name: 'Setup Terraform'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false
      ## TODO: Uncomment and configure AWS credentials
      # - name: 'Configure AWS Credentials'
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: ${{ secrets.AWS_ROLE_ARN_PROD }}
      #     aws-region: ${{ env.AWS_REGION }}

      - name: 'Terraform Init'
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform init -input=false

      - name: 'Terraform Plan'
        id: plan
        run: |
          cd ${{ env.TF_WORKING_DIR }}
          terraform plan -input=false -var-file="envs/terraform_prod.tfvars" -out=tfplan-prod
        continue-on-error: true

      - name: 'Upload Plan Artifact'
        if: steps.plan.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan-prod
          path: ${{ env.TF_WORKING_DIR }}/tfplan-prod
          retention-days: 30
